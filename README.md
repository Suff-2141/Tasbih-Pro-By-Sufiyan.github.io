# Tasbih Pro

Offline-first digital tasbih, built with HTML, CSS, and vanilla ES modules.

`index.html` is the dedicated five-second splash page; it automatically opens `app.html`, which contains the counter and features. Counts and settings save instantly in LocalStorage, while history is mirrored to IndexedDB. Use Settings to export or restore a JSON backup.
